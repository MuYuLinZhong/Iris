你现在是一个10年经验的Python全栈架构师 + LangGraph & LlamaIndex专家，我要让你从零完整构建一个名叫 **Iris** 的开源项目。

### 项目目标
Iris 是一个**私有化、智能化的代码讲解与知识库系统**，专为忙碌架构师设计。
核心功能要求：
1. **手动初始全仓解读**（一次性）：用户通过命令指定版本（tag 或 commit SHA）和模块优先级（high/medium/low），系统对整个仓库进行深度结构化解读并构建知识图。
2. **PR增量演进**（自动）：GitCode/GitHub PR合并后，通过 Webhook 自动只更新改动部分，增量更新知识图谱。
3. **Agentic + GraphRAG 架构**：使用 LangGraph 作为核心 orchestrator，让系统像真人架构师一样动态思考：
   - Router Agent 分类问题类型（局部细节 / 跨模块关系 / 全局风险 / 演进历史）
   - 多工具并行调用 + 自我反思循环（Reflection Node）
   - 如果结果不完整或有幻觉，自动重写查询、切换工具、重新执行
4. **永不爆上下文**：GraphRAG 分层摘要（文件→模块→社区摘要→全仓）+ Agent 只拉取相关子图
5. **自然语言检索**：支持复杂问题，例如“用中级架构师水平解释 payment 模块的Redis限流逻辑，为什么不用数据库？这个改动对 order 模块有什么影响？”
6. **模块优先级**：high=深度架构权衡+关系图+风险分析，medium=核心逻辑+调用链，low=简要功能
7. **LangGraph 可视化**：必须提供 Mermaid 流程图（设计文档中嵌入 + 代码中动态生成）和 FastAPI 可视化端点
8. 支持 GitCode（主力）+ GitHub，无任何定时任务（Cron/APScheduler），数据全私有，一键 Docker 部署。

### 技术栈（必须严格使用以下组合）
- 后端：FastAPI + Uvicorn
- Agent 编排：**LangGraph**（StateGraph + conditional edges + reflection loop，必须完整实现）
- RAG引擎：**LlamaIndex**（PropertyGraphIndex + 社区摘要 + GraphRAG）
- 向量数据库：Chroma
- 图数据库：Neo4j
- Git操作：GitPython
- 代码解析：tree-sitter + tree-sitter-languages
- LLM：默认 Anthropic Claude 3.5 Sonnet（支持轻松切换 Grok / OpenAI）
- 工具：至少4个工具（vector_retrieve、graph_traverse、cypher_query、code_ast_analyze）

### 项目目录结构（必须严格按此生成）
iris/
├── docker-compose.yml
├── config.yaml
├── requirements.txt
├── main.py
├── README.md
├── core/
│   ├── repo_manager.py
│   ├── graph_builder.py
│   └── explainer_agent.py     # LangGraph 主逻辑 + 可视化函数
├── agents/
│   └── init_full_repo.py
├── storage/
│   ├── chroma_db.py
│   └── neo4j_graph.py
├── utils/
│   ├── prompt_templates.py
│   ├── tools.py               # 4个 Agent 工具实现
│   └── visualize.py           # LangGraph 可视化辅助函数
└── data/                      # gitignore


### 生成步骤（必须严格按顺序执行，不得跳过）
**第一步**：先生成一份完整的 **《Iris 项目架构设计文档》**（Markdown格式），必须包含以下章节：
1. 项目概述与目标
2. Agentic + GraphRAG 架构说明（与传统RAG对比）
3. 完整 Agent 流程图（用 Mermaid 语法画出：Start → Router → Tool Executor → Reflection → 循环或 Final Answer）
4. LangGraph State 定义（messages, retrieved_context, graph_results, critique 等）
5. 工具列表及作用
6. 模块优先级处理逻辑
7. LangGraph 可视化实现方案（/visualize-agent 端点 + visualize_graph 函数）
8. config.yaml 字段详细说明
9. 部署与使用手册（包含初始命令、Webhook 配置、测试示例）

**第二步**：等我回复“文档OK”后，再逐个文件生成完整、可直接运行的代码。
- 每个文件生成时，先写“=== 文件路径：xxx ===”和“作用说明”
- 再给出完整代码块（必须加详细中文注释 + 类型提示 + 错误处理 + 日志）
- explainer_agent.py 必须完整实现 StateGraph（router、tool_executor、reflection、conditional edges）
- main.py 必须包含 /visualize-agent 端点（浏览器直接显示 Mermaid 图）
- utils/visualize.py 必须包含 visualize_graph() 函数（支持 draw_mermaid + draw_mermaid_png）

**第三步**：全部代码生成完毕后，输出“一键启动命令总结”和“测试指南”（包含一个复杂问题的 Agent 查询示例）。

现在开始，请**严格按照上面步骤**，先生成《Iris 项目架构设计文档》。开始！